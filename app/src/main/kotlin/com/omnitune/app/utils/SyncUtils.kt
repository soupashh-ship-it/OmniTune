package com.omnitune.app.utils

import com.omnitune.app.db.DatabaseDao

object SyncUtils {
    suspend fun syncData(database: DatabaseDao) {
        // Placeholder for future OmniTune Cloud Sync
    }
}