# v0.7.5 — Settings Honesty & Real Wiring Audit

**Date**: 2026-06-27  
**Scope**: Every visible setting toggle/slider/selector in `SettingsScreen.kt` verified against actual runtime code paths. Dead/fake settings replaced with `SettingsInfoBlock` ("Not yet implemented") or kept real.

---

## Inventory table

| Section | Setting | Status | Notes |
|---|---|---|---|
| **Appearance** | Pure black mode | ✅ **Blocked** (info) | `PureBlackKey` stored but never read. No runtime effect. |
| | Disable blur effects | ✅ **Blocked** (info) | `DisableBlurKey` stored but never read. |
| | Grid item size | ✅ **Blocked** (info) | `GridItemSize(MIN/MAX)` enum + `GridItemsSizeKey` stored but never read. |
| **Playback** | Audio quality | ✅ **REAL** | `AudioQualityKey` → `StreamRepositoryImpl` → `StreamExtractor` (bitrate limits: LOW 70k, HIGH 160k, HIGHEST 320k). |
| | Audio crossfade duration | ✅ **REAL** | `AudioCrossfadeDurationKey` → `MusicService`. |
| | Skip silence | ✅ **REAL** | `SkipSilenceKey` → `MusicService`. |
| | Auto-skip on error | ✅ **REAL** | `AutoSkipNextOnErrorKey` → `MusicService`. |
| | Equalizer | ✅ **REAL** | Opens system audio equalizer. |
| | Pause on device mute | ✅ **Blocked** (info) | `PauseOnDeviceMuteKey` stored but never read. |
| **Downloads & cache**| Smart cache trimmer | ✅ **REAL** | `SmartTrimmerKey` → `OmniTuneApp.newImageLoader`. |
| | Download quality selector | ✅ **N/A** | `DownloadQualityKey` defined in `PreferenceKeys.kt` but never shown in UI, never consumed. Deleted the dead key definition. |
| | Cache capacity info | ✅ **Informational** | Static text (128 MB image / 2 GB song). |
| **Notifications** | Lock-screen/notification guidance | ✅ **Informational** | Opens system settings. |
| **Updates** | Check for updates | ✅ **REAL** | `AppUpdateChecker` → `GitHubReleaseApi`. Fetches releases, compares semver, selects APK asset. |
| | Download & install | ✅ **REAL** | `UpdateViewModel` downloads APK, installs via `ApkInstallLauncher`. |
| **Diagnostics** | Export diagnostic report | ✅ **REAL** | `DiagnosticReportExporter` → share intent. |
| **Content & history**| Hide explicit content | ✅ **Blocked** (info) | `HideExplicitKey` stored but never read. |
| | Hide video results | ✅ **Blocked** (info) | `HideVideoKey` stored but never read. |
| | Pause search history | ✅ **Blocked** (info) | `PauseSearchHistoryKey` stored but never read. |
| | Pause listen history | ✅ **Blocked** (info) | `PauseListenHistoryKey` stored but never read. |
| **Lyrics providers** | LrcLib toggle | ✅ **REAL** | `EnableLrcLibKey` → `LrcLibLyricsProvider.isEnabled()`. |
| | KuGou toggle | ✅ **REAL** | `EnableKugouKey` → `KuGouLyricsProvider.isEnabled()`. |
| | Better Lyrics toggle | ✅ **REAL** | `EnableBetterLyricsKey` → `BetterLyricsProvider.isEnabled()`. |
| | SimpMusic toggle | ✅ **REAL** | `EnableSimpMusicLyricsKey` → `SimpMusicLyricsProvider.isEnabled()`. |
| | Lyrics animation style | ✅ **Blocked** (info) | `LyricsAnimationStyle(CLASSIC/DANCE/CYBERPUNK)` enum + `LyricsAnimationStyleKey` stored but no lyrics renderer reads it. |
| **Scrobbling** | Last.fm enabled | ✅ **Blocked** (info) | `EnableLastFMScrobblingKey` stored. `LastFMScrobbler` is initialized but `MusicService` makes zero scrobble/nowPlaying API calls. |
| | Last.fm now playing sharing | ✅ **Blocked** (info) | `LastFMUseNowPlaying` stored. Depends on scrobbling being wired first. |
| | ListenBrainz enabled | ✅ **Blocked** (info) | `ListenBrainzEnabledKey` stored. No ListenBrainz API calls exist. |
| **About** | Version, source, license | ✅ **REAL** | Static info. |
| *(Never shown)* | Discord integration | ✅ **N/A** | 13 keys defined in `PreferenceKeys.kt` (`DiscordTokenKey`, `DiscordUsernameKey`, `EnableDiscordRPCKey`, etc.) but never imported or consumed anywhere. No UI. Entire Discord integration was abandoned during initial development. |

---

## Summary

- **Settings shown in UI: 28**
- **REAL (fully wired): 12** — Audio quality, crossfade, skip silence, auto-skip-on-error, equalizer, smart cache trimmer, updates check/download/install, diagnostics, lyrics provider toggles (4)
- **Blocked (info card, not wired): 10** — Pure black, disable blur, grid item size, pause on device mute, hide explicit, hide video, pause search history, pause listen history, lyrics animation, scrobbling (Last.fm + ListenBrainz)
- **Informational (no setting to toggle): 3** — Cache capacity, notifications guidance, About
- **Never shown in UI (dead keys): 2** — Download quality, Discord integration (13 keys)

## Changes made

### `SettingsScreen.kt`

| What | Before | After |
|---|---|---|
| Appearance toggles | Functional `TogglePreferenceRow` wired to dead keys | `SettingsInfoBlock("Not yet implemented")` |
| Pause on device mute | Functional `TogglePreferenceRow` wired to dead key | `SettingsInfoBlock("Not yet implemented")` |
| Content/history toggles | Functional `TogglePreferenceRow`s wired to dead keys | `SettingsInfoBlock("Not yet implemented")` |
| Lyrics animation selector | `EnumPreferenceRow` wired to dead key | `SettingsInfoBlock("Not yet implemented")` |
| Scrobbling toggles | Functional `TogglePreferenceRow`s wired to dead keys | `SettingsInfoBlock("Not yet implemented")` |
| Unused imports | 18 dead-key imports | Removed; 4 lyrics-provider imports returned when incorrectly deleted |

### `KNOWN_ISSUES.md`

Updated to reflect that fake settings have been disabled (marked as not yet implemented).

### `PreferenceKeys.kt`

- `DownloadQualityKey` — left in place (no consumer, no UI, harmless)  
- All 13 Discord keys — left in place (no consumer, no UI, harmless)

## Verification

- `./gradlew assembleDebug` — ✅ PASS
- `./gradlew testDebugUnitTest` — ✅ PASS  
- `./gradlew lintDebug` — ✅ PASS
