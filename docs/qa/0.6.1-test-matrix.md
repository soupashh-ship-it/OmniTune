# OmniTune 0.6.1 Stabilization Test Matrix

Date: 2026-06-22
Branch: `main`
Commit: `21cb3f4` plus current QA fixes in worktree
Device: `138898743000055`
Package: `com.omnitune.app.debug`
Release candidate: `0.6.2` / `versionCode 13`

## Build

Status: PASS

- `.\gradlew.bat clean assembleDebug`: PASS before remaining QA.
- `.\gradlew.bat assembleDebug`: PASS after search queue-action patch.
- `.\gradlew.bat assembleDebug`: PASS after active-player download fallback patch.
- `.\gradlew.bat clean assembleDebug`: PASS after process-death playback patch.
- `.\gradlew.bat assembleDebug`: PASS after queue-save cancellation logging patch.
- `.\gradlew.bat clean assembleDebug`: PASS after `0.6.2` version bump.

## Install

Status: PASS

- `adb install -r app\build\outputs\apk\debug\app-debug.apk`: PASS, `Success`.
- `adb install -r app\build\outputs\apk\debug\app-debug.apk`: PASS after `0.6.2` version bump, `Success`.
- `adb shell pm list packages | findstr omnitune`: PASS, found debug package in earlier 0.6.1 QA.

## Launch

Status: PASS

- `adb shell monkey -p com.omnitune.app.debug 1`: PASS.
- App launched to `com.omnitune.app.debug/com.omnitune.app.MainActivity`.
- No fatal OmniTune crash observed in sampled logcat.

## Search

Status: PASS

- Opened Search through ADB.
- Searched `africa`.
- Song results displayed with item overflow menus.

## Playback

Status: PASS

- Tapped `Africa` result.
- MediaSession reported `state=PLAYING(3)`.
- Metadata: `Africa, Toto, Toto IV`.
- Queue size: `1`.
- No endless buffering observed during the smoke test.
- Process-death regression:
  - Played `Faded`.
  - Force-stopped with `adb shell am force-stop com.omnitune.app.debug`.
  - Reopened with `adb shell monkey -p com.omnitune.app.debug 1`.
  - Searched and tapped `Levitating`.
  - MediaSession reported `state=PLAYING(3)`, metadata `Levitating, Dua Lipa, Future Nostalgia`, error `null`.
  - Logcat showed fresh `StreamUrlResolver: resolving OsfAnsMY21M`, `Player setMediaItems`, `Player prepared`, and `Player state: READY`.

## Force-Stop + Reopen

Status: PASS

- Reopen app: PASS.
- Previous session restored safely: PARTIAL.
  - On this ADB run, the previous search-played item was not restored as a MiniPlayer because the persisted queue could not be rebuilt from local database song rows.
  - This is safe behavior for 0.6.1 because no dead MiniPlayer or stale player state was shown.
  - Code path now restores persisted queue metadata only when local song rows are available.
- New search result plays after force stop: PASS.
- Stale stream URL not reused: PASS.
  - Service startup clears the in-memory stream cache.
  - Restored queue items are normalized back to original video IDs before any resumed playback.
  - The post-force-stop `Levitating` test performed a fresh stream resolution.
- No endless buffering: PASS.
- No AndroidRuntime crash or PlayerError observed in sampled force-stop logcat.

## MiniPlayer

Status: PASS

- MiniPlayer appeared during playback.
- MiniPlayer showed `Africa` metadata and active pause state.
- MiniPlayer body opened the full player.
- Play/pause via MiniPlayer was previously verified in this 0.6.1 pass and stayed consistent with MediaSession.

## Full Player

Status: PASS

- Full player opened from MiniPlayer.
- Title/artist/artwork/progress rendered.
- Full-player controls rendered with active `Pause`, `Previous`, `Next`, `Download`, and `Queue` actions.

## Queue / Play Next / Add to Queue

Status: PASS

- Added search-row overflow menu actions for `Play next` and `Add to queue`.
- Played song A, added song B with `Add to queue`, and MediaSession queue size changed to `2`.
- Pressed media next and confirmed song B became current.
- Used `Play next` for song C, queue size changed to `3`, and the next media key advanced to song C.
- Queue order stayed stable; no index shift or silent item drop was observed in this runtime test.

## Notification Shade / MediaSession / Lock Screen

Status: PARTIAL

- MediaSession external controls PASS:
  - `KEYCODE_MEDIA_PLAY_PAUSE` changed playback from `PLAYING(3)` to `PAUSED(2)`.
  - A second media key event resumed `PLAYING(3)`.
  - Screen-off media key also reached OmniTune and resumed playback with correct metadata.
- Notification shade visual controls PARTIAL:
  - `POST_NOTIFICATIONS` is granted.
  - `music_player` notification channel exists.
  - `dumpsys notification` did not show an active OmniTune media notification on this Vivo/OEM device during the ADB check.
  - Manual shade and lock-screen visual control tapping is still needed.

## Download / Offline

Status: PARTIAL

- Fixed active-player Download button for search-played tracks that do not yet have a database `SongEntity`.
- Fixed active-player download to reuse the current resolved ExoPlayer stream URL when available instead of re-resolving and failing while playback is already working.
- Runtime result:
  - Download action created Media3 cache data under `files/downloads/5/0.0.1782125703885.v3.exo`.
  - Cache file grew to about `2.3M`.
- Offline playback after force-stop is NOT fully verified:
  - `adb shell svc wifi disable` and `adb shell svc data disable` did not create a clean offline state because the device kept a validated VPN/cellular path alive.
  - After force-stop, the app returned to Home without a restored playable item available from ADB.

## Error States

Status: PARTIAL

- Download failure is user-visible through the existing callback/toast path.
- Download start no longer silently fails when the active track has metadata but no local song row.
- Network-disabled search/playback error states were not cleanly verified because the device retained VPN/cellular connectivity during ADB network-disable attempts.

## Remaining Release Blockers

- True offline playback needs a manual phone test with VPN disabled and all network disabled.
- Notification shade and lock-screen visual controls need manual tapping on the device.
- The `0.6.2` debug APK was built from this worktree; production release signing is still separate.

## Manual Tests Still Needed

- Disable VPN manually, then disable Wi-Fi and mobile data.
- Open Downloads and confirm the downloaded `Africa` item is listed as downloaded.
- Force-stop, relaunch, and play the downloaded item with all network disabled.
- Pull the notification shade during playback and tap play/pause/next/previous.
- Lock the phone during playback and test lock-screen visual controls.
