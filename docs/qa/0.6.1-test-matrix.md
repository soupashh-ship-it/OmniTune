# OmniTune 0.6.3 Stabilization Test Matrix

Date: 2026-06-22
Branch: `main`
Base commit: `cb1d4f4` / tag `v0.6.2`, plus 0.6.3 stabilization fixes
Device: `138898743000055`
Package: `com.omnitune.app.debug`
Version under test: `0.6.3` / `versionCode 14`

## Build

Status: PASS

- `.\gradlew.bat clean assembleDebug`: PASS before final QA.
- `.\gradlew.bat assembleDebug`: PASS after NewPipeExtractor/Rhino alignment, search list performance patch, and Downloads playback handoff.
- `.\gradlew.bat :app:dependencies --configuration debugRuntimeClasspath`: confirmed `com.github.teamnewpipe:newpipeextractor:v0.26.3` with `org.mozilla:rhino:1.8.1`.

## Install

Status: PASS

- `adb install -r app\build\outputs\apk\debug\app-debug.apk`: PASS, `Success`.

## Launch

Status: PASS

- `adb shell monkey -p com.omnitune.app.debug 1`: PASS.
- App launched to `com.omnitune.app.debug/com.omnitune.app.MainActivity`.
- No OmniTune `AndroidRuntime` crash observed in sampled logcat.

## Search

Status: PASS

- Searched `levitating`, `faded`, `arijit singh`, and `shape of you`.
- Results rendered and search-row taps reached playback callbacks.
- Some source-specific YouTube failures can still occur, but common tested items resolved after extractor alignment.

## Search Scroll Performance

Status: PASS

- Added stable LazyColumn keys/content types and bounded 96x96 artwork requests for search rows.
- Fast-scrolled `arijit singh` and `shape of you` results.
- `adb shell dumpsys gfxinfo com.omnitune.app.debug reset` sample after fast scrolling:
  - Total frames: `342`
  - Janky frames: `18` / `5.26%`
  - 50th percentile: `11ms`
  - 90th percentile: `19ms`
  - 95th percentile: `34ms`
  - Slow bitmap uploads: `0`
- Tapping a result after scrolling still triggered playback.

## Playback

Status: PASS

- `Levitating` played after stream resolution:
  - `StreamUrlResolver: resolved OsfAnsMY21M`
  - `Player setMediaItems`
  - `Player state: READY`
  - MediaSession `PLAYING(3)`, metadata `Levitating, Dua Lipa, Future Nostalgia`.
- `Faded` played after force-stop/reopen:
  - MediaSession `PLAYING(3)`, metadata `Faded, Alan Walker, Different World`.

## Force-Stop + Reopen

Status: PASS

- Played `Levitating`.
- Force-stopped with `adb shell am force-stop com.omnitune.app.debug`.
- Reopened with `adb shell monkey -p com.omnitune.app.debug 1`.
- Searched and tapped `Faded`.
- Result: fresh stream resolution, `Player state: READY`, MediaSession `PLAYING(3)`.
- No stale direct stream URL reuse observed; service startup cleared stream memory cache.
- No dead MiniPlayer or endless buffering observed.

## MiniPlayer

Status: PASS

- MiniPlayer appeared during active playback.
- MiniPlayer showed active metadata and play/pause state.
- MiniPlayer opened the full player.
- MiniPlayer state matched MediaSession during sampled playback.

## Full Player

Status: PASS

- Full player opened from MiniPlayer.
- Title, artwork area, progress, play/pause, previous, next, download, and queue controls rendered.
- Active-player Download action queued a Media3 download for the current track.

## Queue / Play Next / Add to Queue

Status: PASS

- Previous 0.6.x QA verified Add to Queue and Play Next through search-row overflow actions.
- Queue order stayed stable and next advanced to the queued item.
- This pass did not change queue/playback core.

## Notification Shade / MediaSession / Lock Screen

Status: PARTIAL

- MediaSession external controls PASS:
  - `adb shell input keyevent 127` paused playback: MediaSession `PAUSED(2)`.
  - `adb shell input keyevent 126` resumed playback: MediaSession `PLAYING(3)`.
  - Metadata stayed `Faded, Alan Walker, Different World`.
- Notification setup PASS:
  - `POST_NOTIFICATION: allow`.
  - `music_player` notification channel exists.
- Notification shade visual controls PARTIAL:
  - `dumpsys notification` on this Vivo/OEM build did not expose a clean active OmniTune notification row.
  - Manual shade tapping still needs physical verification.
- Lock-screen visual controls PARTIAL:
  - Media-key routing works through MediaSession.
  - Physical lock-screen artwork/buttons still need manual phone verification.

## Download / Offline

Status: PASS

- Tapped Download for `Faded`.
- Media3 download index showed:
  - `pIWaVJPl0-c|3|100.0|3531890|3531890|0|0`
  - `state=3` is completed.
- Force-stopped app.
- Disabled Wi-Fi and mobile data through ADB.
- Reopened app.
- Navigated to Library > Downloads.
- Tapped completed `Faded` row.
- Result:
  - `StreamUrlResolver: offline cache hit for pIWaVJPl0-c`
  - `Player setMediaItems`
  - `Player state: READY`
  - MediaSession `PLAYING(3)`
- Metadata limitation: offline playback currently restores title from download data; artist/album/artwork are not preserved in the download row yet.

## Error States

Status: PARTIAL

- Download incomplete/failed rows are not treated as playable.
- Completed Downloads rows are playable; failed rows retain retry/remove actions.
- Network-disabled playback of downloaded content PASS.
- Network-disabled search and non-downloaded playback user-facing error copy still needs a manual UI pass.

## Release Hygiene

Status: PASS

- `git ls-files app/keystore.jks app/google-services.json *.apk *.idsig *.zip crash_log.txt`: no tracked files.
- `.gitignore` ignores `*.jks`, `*.keystore`, `google-services.json`, `*.apk`, `*.idsig`, `*.zip`, and `crash_log.txt`.
- Firebase remains opt-in.
- Release signing does not fall back to debug signing.
- Velune attribution remains in `CREDITS.md`.

## Remaining Release Blockers

- Physical notification shade controls need a manual phone tap test.
- Physical lock-screen controls need a manual phone tap test.
- Network-disabled search and non-downloaded playback errors need a manual UI pass.
- Offline downloads now play, but richer offline metadata/artwork should be improved in a future stabilization patch.

## Manual Tests Still Needed

- Pull the notification shade during playback and tap pause/play/next/previous.
- Lock the phone during playback and test lock-screen pause/play/next.
- Disable internet and attempt a new search; confirm the visible error/cached state copy is acceptable.
- Disable internet and attempt a non-downloaded song; confirm no endless buffering and user-visible failure.
