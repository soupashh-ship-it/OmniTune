# OmniTune 0.6.1 Stabilization Test Matrix

Date: 2026-06-22
Branch: `main`
Commit: `7a29166`
Device: `138898743000055`
Package: `com.omnitune.app.debug`

## Build

Status: PASS

- `.\gradlew.bat assembleDebug`: PASS before final QA pass.
- `.\gradlew.bat assembleDebug`: PASS after download/error feedback patch.
- `.\gradlew.bat clean assembleDebug`: PASS after final QA documentation update.

## Install

Status: PASS

- `adb install -r app\build\outputs\apk\debug\app-debug.apk`: PASS, `Success`.
- `adb shell pm list packages | findstr omnitune`: PASS, found `package:com.omnitune.app.debug`.

## Launch

Status: PASS

- `adb shell monkey -p com.omnitune.app.debug 1`: PASS.
- Foreground activity: `com.omnitune.app.debug/com.omnitune.app.MainActivity`.
- Startup crash check: no fatal OmniTune crash observed in sampled logcat.

## Search

Status: PASS

- Opened Search through ADB.
- Searched `never gonna give you up`.
- Results displayed with song rows including `Never Gonna Give You Up (Rick Astley)`.

## Playback

Status: PASS

- Tapped first search result.
- MediaSession reported `state=PLAYING(3)`.
- Metadata: `Never Gonna Give You Up (Rick Astley), Stegosaurus Rex, The Dino Soars`.
- Queue size: `1`.
- No endless buffering observed during the smoke test.
- Final installed APK was re-smoked after `clean assembleDebug`; MediaSession again reported `state=PLAYING(3)` with the same metadata and queue size `1`.

## MiniPlayer

Status: PASS

- MiniPlayer appears after playback starts.
- Shows current title and active play/pause state.
- Pause button changed MediaSession to `PAUSED(2)` without opening full player.
- Play button resumed MediaSession to `PLAYING(3)`.
- Body tap opened the full player.
- Next button was disabled because the queue had one item.

## Full Player

Status: PASS

- Full player opened from MiniPlayer.
- Title/artist/artwork/progress rendered.
- Full-player play/pause worked and matched MediaSession state.
- Queue button opened the queue screen.

## Queue

Status: PARTIAL

- Queue screen showed the current item under `Now Playing`.
- Queue screen showed `Up Next (0)` and `No upcoming items`, matching MediaSession queue size `1`.
- Add-to-queue and play-next were not end-to-end verified because the visible search result rows in this test did not expose those item actions.

## Notification Controls

Status: PARTIAL

- MediaSession was active and accepted external play/pause via media key events in earlier runtime checks.
- Notification channels for media playback exist.
- Notification shade controls and lock-screen controls were not manually tapped in this pass.

## Download/Offline

Status: PARTIAL

- Download/cache code path was patched to avoid fake `file:///offline` playback and use Media3 download request URI plus `customCacheKey = videoId`.
- `ExoDownloadService` listener registration is guarded against duplicates.
- Player download action now reports async success/failure instead of immediately showing a false `Download started` toast.
- If the active song object is missing, the player now shows `No active song to download`.
- ADB tap on the full-player Download button did not produce an observable download service/log entry during this pass, so completed offline playback is NOT verified.

## Error States

Status: PARTIAL

- Search displayed results rather than a blank page for a normal query.
- Download start now has visible failure paths for unresolved stream or missing active song.
- Network-disabled search/playback and unavailable-stream playback were not exhaustively tested.

## Remaining Risks

- Offline/download still needs a full phone test: download completion, app restart, network disabled, offline playback.
- Add-to-queue and play-next need manual verification through any UI menu that exposes those actions.
- Notification shade and lock-screen controls need manual verification on the device.
- Search orchestration is improved but still mostly in `SearchViewModel`; repository extraction remains future cleanup.

## Manual Tests Still Needed

- Long-press or menu-test a second search result and verify Add to Queue.
- Use Play Next on a third song and confirm queue order.
- Pull notification shade during playback and tap play/pause/next/previous.
- Lock the phone during playback and test lock-screen controls.
- Tap Download on the phone, wait for completion, kill the app, disable Wi-Fi/data, relaunch, and play the downloaded song.
